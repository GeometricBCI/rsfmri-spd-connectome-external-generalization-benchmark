# Preprocessing configurations

The release preserves the established OAS connectome, tangent-space, and
split-local harmonization behavior. Preprocessing variants should be added only
with an explicit scientific-methods review; the unified configuration currently
selects the atlas and harmonization mode without changing mathematical defaults.
